# CodeRabbit Demo Repo

This repository is intentionally flawed to provoke useful AI review comments. You will open pull requests that fix or break things and observe how CodeRabbit responds.

## How to run
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt || pip install pytest
pytest
